# Railway Production Environment Variables Template
# Copy these to your Railway project dashboard

# =======================
# ESSENTIAL VARIABLES (Required for basic functionality)
# =======================

# Database (Railway will provide this automatically when you add PostgreSQL)
DATABASE_URL=postgresql://username:password@hostname:port/database

# Application Settings
NODE_ENV=production
PORT=3000

# JWT Security (GENERATE A SECURE RANDOM STRING!)
JWT_SECRET=your-super-secret-jwt-key-change-this-to-random-string
JWT_EXPIRES_IN=7d

# CORS Configuration (Update with your actual frontend domain)
FRONTEND_URL=https://your-frontend-domain.com
ALLOWED_ORIGINS=https://your-frontend-domain.com

# =======================
# OPTIONAL VARIABLES (For enhanced functionality)
# =======================

# AI Research Capabilities (required for AI features)
OPENAI_API_KEY=sk-your-openai-api-key-here
SERP_API_KEY=your-serp-api-key-here

# Redis (add Redis addon in Railway for caching and background jobs)
REDIS_URL=redis://default:password@hostname:port

# Rate Limiting
API_RATE_LIMIT_WINDOW_MS=900000
API_RATE_LIMIT_MAX_REQUESTS=100

# Background Jobs
QUEUE_REDIS_URL=redis://default:password@hostname:port
QUEUE_CONCURRENCY=5

# Email Configuration (for notifications)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-app-password

# Security
SESSION_SECRET=another-secure-random-string

# File Upload
MAX_FILE_SIZE=10485760
UPLOAD_PATH=/tmp/uploads

# Monitoring and Logging
LOG_LEVEL=info
ENABLE_METRICS=true

# =======================
# DEPLOYMENT CHECKLIST
# =======================
# ✅ Add PostgreSQL database in Railway dashboard
# ✅ Set DATABASE_URL (auto-provided)
# ✅ Generate secure JWT_SECRET
# ✅ Set NODE_ENV=production
# ✅ Set FRONTEND_URL to your actual domain
# ✅ Add OpenAI API key (if using AI features)
# ⚠️  Add Redis addon (optional, for background jobs)
# ⚠️  Configure SMTP for email notifications (optional)
